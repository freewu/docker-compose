# Pulsar 外部配置（镜像版本 / HOST_IP / JVM 内存）
# 生效方式：改完执行 docker compose up -d --force-recreate

# 镜像版本（apachepulsar/pulsar）
#   4.2.4 是当前稳定版（4.x 线；4.0.13、3.3.9 也可以）
#   带内置 connector / functions 运行时的全量包叫 apachepulsar/pulsar-all（体积大很多，要跑内置 source/sink 时才需要）
#   报 manifest unknown 说明这个 tag 不存在，去 Docker Hub 上挑一个存在的 tag
PULSAR_VERSION=4.2.4

# 宿主机局域网 IP（必填，不能是 127.0.0.1 / localhost / 容器 IP）
# 作用：作为 standalone 的 --advertised-address —— broker 在 topic lookup 时告诉客户端「你接下来连我这个地址」。
#   * 只有宿主机本机的客户端连：填 localhost 也能用（standalone 默认就是 localhost）
#   * 要让局域网里其它机器、或别的容器里的应用连：必须填宿主机局域网 IP，
#     否则客户端 lookup 后会拿到 localhost:6650，然后连自己的本机 → 报 Connection refused
#   * 探测当前 IP：../redis-cluster/set-host-ip.sh --print
HOST_IP=192.168.110.141

# JVM 内存（bin/pulsar 认 PULSAR_MEM；不填就用镜像默认的 -Xms2g -Xmx2g -XX:MaxDirectMemorySize=4g）
#   * MaxDirectMemorySize 同时决定 BookKeeper 读写缓存大小（约为它的 1/8），给太小会报 Direct buffer memory，
#     建议不要低于 512m；机器内存紧张时优先调小 -Xmx
PULSAR_MEM=-Xms512m -Xmx1g -XX:MaxDirectMemorySize=1g
