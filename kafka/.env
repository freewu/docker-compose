# Kafka 环境变量
# 作用：KAFKA_ADVERTISED_LISTENERS 里用的宿主机局域网 IP（broker 通告给客户端的连接地址）
# 生效方式：修改后 docker compose up -d --force-recreate kafka9094
#          （或整体 docker compose up -d）

# 宿主机局域网 IP（必填，不能是 127.0.0.1 / localhost）
#   * 本机 IP 变了要记得改这里，否则客户端能连上 broker，但生产/消费时会报
#     "Connection to node 0 (xxx) could not be established"
#   * 探测当前 IP：../redis-cluster/set-host-ip.sh --print
HOST_IP=192.168.0.200
