# Redis Cluster 环境变量（本文件随仓库提交，仅存放开发环境配置）
#
# HOST_IP：宿主机局域网 IP，集群节点用它作为对外通告地址。
#   必须同时满足「容器之间能访问」和「宿主机上的客户端能访问」，
#   所以不能写 127.0.0.1（6 个节点是独立容器，写 127.0.0.1 会导致节点互相连不上/客户端重定向失败）。
#
# 不用手动填：执行 ./set-host-ip.sh 会自动探测本机 IP 并更新下面这行
#   手动指定：  ./set-host-ip.sh 192.168.1.60
#   只看不写：  ./set-host-ip.sh --print
#   Windows 用 ipconfig 查看 IPv4 地址，Linux 用 hostname -I 查看。
#
# 注意：IP 变了要重新执行脚本，并重建集群（docker compose down && 清空 /data/redis-cluster/* && up -d）。
HOST_IP=192.168.0.200
